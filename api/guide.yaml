# config env
DATABASE_URL
JWT_SECRET_KEY
EMAIL_HOST
PASSWORD_EMAIL_HOST
BASE_URL

# random key for jwt_serect_key
openssl rand -base64 32

# run console ninja
console-ninja node --watch app.js

# push db in mongodb
npx prisma db push

# run project
- npm install
- npm run start
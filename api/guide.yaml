# config env
DATABASE_URL
JWT_SECRET_KEY

# random key for jwt_serect_key
openssl rand -base64 32

# run console ninja
console-ninja node --watch app.js

# push db in mongodb
npx prisma db push

# run project
- npm intall
- npm run start